@model IEnumerable<ProyectoControlDePermisos.Models.AutorizacionPermiso>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPermiso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEmpleadoAutorizador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaAutorizacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comentarios)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPermiso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleadoAutorizador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaAutorizacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comentarios)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAutorizacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAutorizacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAutorizacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
